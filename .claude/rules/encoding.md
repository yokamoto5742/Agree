- 日本語を含むファイルの作成・編集は Write / Edit ツールで行う。Bash の `printf` / `echo` / `cat <<EOF` / `>>` でファイルへ日本語を書き込まない（Git Bash 経由だと UTF-8 で保存されず文字化けする）
- Bash をファイル編集に使う場合は ASCII のみのコマンド（`head` / `sed` での行削除など）に限る
- 日本語を書き込んだ後は `file <パス>` で UTF-8 であることと `git diff` で文字化けがないことを確認する
